<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/public/css/styles.css">
    <title>Todo-List</title>
  </head>
  <body> -->
    <%- include('header'); -%>

    <div class="box" id="heading">
      <h1><%= listTitle %></h1>
    </div>
   

    <!-- <ul> -->
        <!-- <li>Buy Food</li>
        <li>Cook Food</li>
        <li>Eat Food</li> -->
       <!-- <% for (let item of newListItems){ %>
            <li><%= item %></li>
      <%  } %>
    </ul> -->
    <div class="box">
      <% newListItems.forEach((item)=>{ %>
        <form action="/delete" method="post">
        <div class="item">
          <input type="checkbox" name="checkName" value="<%=item._id%>" onchange="this.form.submit()">
          <!-- onchange="this.form.submit()"  will helps us to have type of submit though having another type -->
          <!-- without value checkbox default value will be on -->
          <p><%= item.name %></p>
          <input type="hidden" name="listName" value="<%=listTitle%>"/>
        </div>
      </form>
    <%   }) %>
        
  

  <form class="item" action="/" method="post">
    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
    <!-- <button type="submit">+</button> -->
    <button type="submit" name="list" value="<%= listTitle %>" >+</button>
    <!-- button type = submit will make a post request to "/" route  
    req.body.name(list) ==> value(listTitle ) -->
</form>
    </div>

  
    <%- include('footer'); -%>
